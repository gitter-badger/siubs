<%= form_for(@parse) do |f| %>
  <% if @parse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parse.errors.count, "error") %> prohibited this parse from being saved:</h2>

      <ul>
      <% @parse.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
