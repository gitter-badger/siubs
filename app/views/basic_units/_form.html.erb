<%= form_for(@basic_unit) do |f| %>
  <% if @basic_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@basic_unit.errors.count, "error") %> prohibited this basic_unit from being saved:</h2>

      <ul>
      <% @basic_unit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
